interface Props {
  currentFolder: any;
  folderName: string;
  workspaceName: string;
}

export function WorkspaceNewFolder({
  currentFolder,
  folderName,
  workspaceName,
}: Props) {
  return <div className=""></div>;
}
